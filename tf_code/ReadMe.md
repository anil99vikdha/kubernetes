# Once terraform apply is completed.
`terraform init`
`terraform plan`
`terraform apply`
`terraform destroy`

## Run the below command from your system
`aws eks update-kubeconfig --region <region> --name <EKS Cluster Name>`

## To get nodes
`kubectl get nodes`

## To list pods from all namespaces
`kubectl get pods -A`